新增了一些功能：
一、可以读入文件路径，而不是只读一个固定文件，同时找不到文件的时候会报错
二、可以读入输出等级，1-4级分别对应：
	①基础要求：输出关键字统计信息
	②进阶要求：输出有几组switch case结构，同时输出每组对应的case个数
		    同时若没有switch结构的时候会输出相应信息	
	③拔高要求：输出有几组if else结构
	④终极要求：输出有几组if，else if，else结构
       同时输入错误等级的时候会报错